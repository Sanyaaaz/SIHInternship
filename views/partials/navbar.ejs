<nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <a href="/" class="flex items-center space-x-2 group">
                <div class="bg-primary p-2 rounded-lg group-hover:bg-primary/90 transition-colors">
                    <i data-lucide="briefcase" class="h-6 w-6 text-white"></i>
                </div>
                <span class="text-xl font-bold text-gray-900">InternshipHub</span>
            </a>

            <!-- Navigation Links -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="<%= currentPath === '/' ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    Home
                </a>
                <a href="/internships" class="<%= currentPath.startsWith('/internships') ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    Internships
                </a>
                <a href="/about" class="<%= currentPath === '/about' ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    About
                </a>
            </div>

            <!-- Auth Buttons -->
            <div class="flex items-center space-x-4">
                <% if (user) { %>
                    <div class="flex items-center space-x-4">
                        <a href="/dashboard" class="<%= currentPath.startsWith('/dashboard') ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> flex items-center space-x-1 px-3 py-2 rounded-md text-sm font-medium transition-colors">
                            <i data-lucide="layout-dashboard" class="h-4 w-4"></i>
                            <span class="hidden sm:inline">Dashboard</span>
                        </a>
                        <div class="flex items-center space-x-2 px-3 py-2 bg-gray-50 rounded-md">
                            <i data-lucide="user" class="h-4 w-4 text-gray-600"></i>
                            <span class="text-sm font-medium text-gray-900 hidden sm:inline"><%= user.name %></span>
                            <span class="text-xs text-gray-500 bg-gray-200 px-2 py-1 rounded-full capitalize"><%= user.role %></span>
                        </div>
                        <form action="/auth/logout" method="POST" class="inline">
                            <button type="submit" class="flex items-center space-x-1 px-3 py-2 text-sm font-medium text-red-600 hover:text-red-700 hover:bg-red-50 rounded-md transition-colors">
                                <i data-lucide="log-out" class="h-4 w-4"></i>
                                <span class="hidden sm:inline">Logout</span>
                            </button>
                        </form>
                    </div>
                <% } else { %>
                    <div class="flex items-center space-x-3">
                        <a href="/auth/login" class="flex items-center space-x-1 px-4 py-2 text-sm font-medium text-gray-700 hover:text-primary transition-colors">
                            <i data-lucide="log-in" class="h-4 w-4"></i>
                            <span>Login</span>
                        </a>
                        <a href="/auth/signup" class="flex items-center space-x-1 px-4 py-2 bg-primary text-white text-sm font-medium rounded-md hover:bg-primary/90 transition-colors">
                            <i data-lucide="user-plus" class="h-4 w-4"></i>
                            <span>Sign Up</span>
                        </a>
                    </div>
                <% } %>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden">
                <button type="button" class="text-gray-700 hover:text-primary" onclick="toggleMobileMenu()">
                    <i data-lucide="menu" class="h-6 w-6"></i>
                </button>
            </div>
        </div>

        <!-- Mobile menu -->
        <div id="mobile-menu" class="hidden md:hidden pb-4">
            <div class="space-y-2">
                <a href="/" class="<%= currentPath === '/' ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    Home
                </a>
                <a href="/internships" class="<%= currentPath.startsWith('/internships') ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    Internships
                </a>
                <a href="/about" class="<%= currentPath === '/about' ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                    About
                </a>
                <% if (user) { %>
                    <a href="/dashboard" class="<%= currentPath.startsWith('/dashboard') ? 'text-primary bg-primary/10' : 'text-gray-700 hover:text-primary hover:bg-gray-50' %> block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                        Dashboard
                    </a>
                <% } else { %>
                    <a href="/auth/login" class="text-gray-700 hover:text-primary hover:bg-gray-50 block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                        Login
                    </a>
                    <a href="/auth/signup" class="text-gray-700 hover:text-primary hover:bg-gray-50 block px-3 py-2 rounded-md text-sm font-medium transition-colors">
                        Sign Up
                    </a>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<script>
    function toggleMobileMenu() {
        const menu = document.getElementById('mobile-menu');
        menu.classList.toggle('hidden');
    }
</script>